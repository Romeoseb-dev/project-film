import React from "react";

const SearchBar = ({titre}) => {
    return (
        <input type="text" placeholder="Recherchez un film..." className="search-bar"/>

        )

}

export default SearchBar